const score = document.querySelector("score");
const btn = document.querySelector("button");


btn.addEventListener("click", (e) => {
    console.log(e.target);
    //score.innerHTML = Math.floor(Math.random() * 6);
})